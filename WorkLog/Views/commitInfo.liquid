<div>
    <span>{{ model.date }}</span>
    <span>
        <a href="{{ model.issue_url }}">{{ model.issue_id }}</a>
    </span>
    <span>
        <a href="{{ model.url }}" title="{{ model.id }}">Commit Details</a>
    </span>
    <span>{{ model.message }}</span>
</div>